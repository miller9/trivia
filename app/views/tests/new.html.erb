<h1> Create your trivia! </h1>

<%= form_for :test, url: tests_path do |f| %>
	<p>
		<%= f.label :question %> <br>
		<%= f.text_field :question %>		
	</p>

	<p>
		<%= f.label :answer %> <br>
		<%= f.text_area :answer %>		
	</p>

	<p>
		<%= f.submit %>
	</p>

<% end %>